# Gemini Adapter: Design Planning

Execute `design-planning` as a review-gated harness. Use the portable core files as authority. Keep planning artifacts under `tasks/design` by default and detailed design documents under `docs/design`. Produce documentation-only detailed design tasks and one final design acceptance task. Do not implement product code.
