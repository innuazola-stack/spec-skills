# Hermas Adapter: Design Planning

Execute the review-gated `design-planning` harness. Read the core files, then the target PRD/HLD under `docs/`. Write planning under `tasks/design` by default and detailed design documents under `docs/design`. Generate documentation-only tasks, fixtures, DAG, and final design acceptance.
