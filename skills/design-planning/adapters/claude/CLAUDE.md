# Claude Adapter: Design Planning

Follow the `design-planning` harness core exactly.

Before planning, read `manifest.yaml`, `agent.md`, `workflow.md`, `rules.md`, `schemas/design-planning.schema.json`, and `skill/SKILL.md`. Then read the target project's PRD/HLD under `docs/`.

Default planning output root: `tasks/design`.

Fixed detailed-design document root: `docs/design`.

Produce planning JSON, DAG, documentation-only task fixtures, exact `docs/design/*.md` design document paths, and one final design acceptance task. Do not implement code or modify production source, tests, manifests, schemas, migrations, runtime scripts, or generated runtime artifacts.
