# OpenClaw Adapter: Design Planning

Run the `design-planning` harness from the portable core files. Default planning root is `tasks/design`; detailed design documents are fixed under `docs/design`. The output is planning and detailed-design documentation only, never implementation code.
