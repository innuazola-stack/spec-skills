# Codex Adapter Install

Install by exposing `skill/SKILL.md` as the Codex discovery entrypoint and keeping the harness core files beside it. Codex must treat `manifest.yaml`, `agent.md`, `workflow.md`, `rules.md`, schemas, validators, and eval cases as the source of truth.
