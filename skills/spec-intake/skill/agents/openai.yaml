display_name: Spec Intake
short_description: Run review-gated PRD intake from idea to requirement table, PRDs, and HLD.
default_prompt: Run the spec-intake harness for this idea: create the requirement table first, render Human and Agent PRDs, wait for Human PRD approval before Stage 3, then produce contract-backed HLD only when gates pass. Do not produce task plans.
