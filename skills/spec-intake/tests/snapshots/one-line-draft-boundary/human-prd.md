# Human PRD Draft Snapshot

Source contract: `contract-envelope.json`

Render status: `draft`

## 目标

这是一个面向销售用户的线索评分工具想法，但当前只有一句话，不能推导 CRM、字段、评分模型、阈值、权限或指标。

## 开放问题

`Q-001` 需要明确第一版依赖的线索输入、评分规则、验证案例、停止条件和完成标准。

## 当前结论

Human PRD 可以作为 draft 讨论，Agent PRD 和任务计划必须保持 blocked。
