# Agent PRD Draft Fixture

Status: draft.

Source contract: `contract-envelope.json`

This fixture must not be consumed for execution.

## Source of Truth

Use `contract-envelope.json`. `GATE-001` blocks execution readiness.

## Blocked Execution Status

The Agent PRD is draft because `IN`, `EXE`, `VER`, `OUT`, `STOP`, and `DONE` objects have not been defined.
